1. List(목록) 

  - List를 사용하기 위한 자료 구조가 Array(배열) 이다.
    배열은 JavaScript의 변수나 객체들을 하나의 변수로 묶어 놓은 것

2. Key(열쇠)

  - 열쇠는 다 각각의 모양을 갖고 있다 즉, 열쇠는 각자 고유하다라고 할 수 있다.
  - 각 객체나 아이템을 구분할 수 있는 고유한 값
  - React에서의 Key는 아이템들을 구분하기 위한 고유한 문자열이다. 

3. 여러 개의 Component 렌더링하기

  - React에서는 Array 와 Key를 통해서 다수의 Element가 렌더링할 수 있다.
  - 여기서 JavaScript에서 사용되는 map() 함수를 통해서 사용할 수 있다.
    mapping의 개념과 비슷하다 mapping은 한쪽에 있는 아이템과 다른 한쪽에 있는 아이템을 짝지어주는 것
    map()은 Array에 있는 변수에 어떠한 처리를 한 뒤에 리턴을 시켜주는 것

=> React에서의 Key는 Key의 값은 같은 List에 있는 Element사이에서만 고유한 값이면 된다. 
  (Key의 고유한 값은 특정 범위에서만 고유하기 때문이다.)
  (ex) 주민등록번호, 여권번호 => 결국 대한민국에서만 고유하지 외국에서는 고유한 값이 아니다.
  결국 속한 집단에서만 고유하면 된다.

  반드시 기억해야되는 건 map()함수 안에 있는 Element는 꼭 Key값이 필요하다!